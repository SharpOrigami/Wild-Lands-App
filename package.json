{
  "name": "wild-lands-game",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --format=esm --jsx=react-jsx --platform=browser --define:process.env.NODE_ENV='\"production\"'"
  },
  "devDependencies": {
    "esbuild": "^0.x.x", // Use the version you installed
    "typescript": "^5.x.x" // Use the version you installed
  }
}
